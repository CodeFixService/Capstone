@page
@model SmartFlow.Web.Pages.Admin.Roles.IndexModel
@{
    ViewData["Title"] = "Mantenedor de Roles";
}

<h2 class="mb-3">Roles Registrados</h2>

<a asp-page="Create" class="btn btn-primary mb-3">
    <i class="bi bi-shield-lock"></i> Nuevo Rol
</a>

<table class="table table-bordered table-hover align-middle">
    <thead class="table-light">
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th class="text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.ListaRoles != null && Model.ListaRoles.Count > 0)
        {
            foreach (var rol in Model.ListaRoles)
            {
                <tr>
                    <td>@rol.Id</td>
                    <td>@rol.Nombre</td>
                    <td>@rol.Descripcion</td>
                    <td class="text-center">
                        <a asp-page="Details" asp-route-id="@rol.Id" class="btn btn-sm btn-outline-info me-1" title="Detalles">
                            <i class="bi bi-eye"></i>
                        </a>
                        <a asp-page="Edit" asp-route-id="@rol.Id" class="btn btn-sm btn-outline-warning me-1" title="Editar">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-page="Delete" asp-route-id="@rol.Id" class="btn btn-sm btn-outline-danger" title="Eliminar">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="4" class="text-center text-muted">No hay roles registrados.</td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
}
